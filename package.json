{
  "name": "incremental-reading",
  "version": "1.0.0",
  "description": "Incremental reading with scheduled topics, extracts, and Spaced Repetition flashcards.",
  "main": "main.js",
  "scripts": {
    "test": "node --test test/*.test.js",
    "validate": "node scripts/validate-release.mjs",
    "check": "node --check main.js && node --check tree-core.js && node --check spaced-repetition-core.js && npm run validate && npm test",
    "build": "npm run check"
  },
  "keywords": [
    "obsidian",
    "incremental-reading",
    "spaced-repetition",
    "flashcards"
  ],
  "author": "kiera",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
